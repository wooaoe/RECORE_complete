selectProdReview=SELECT REVIEW.*, ACCOUNT.ACC_ID FROM PROD_OPTION LEFT JOIN REVIEW ON(PROD_OPTION.PROD_ID = REVIEW.PROD_ID) LEFT JOIN ORDER_NUM ON(REVIEW.ORDER_NO = ORDER_NUM.ORDER_NO) LEFT JOIN ACCOUNT USING(ACC_NO) WHERE PROD_OPTION.PROD_NO = ? ORDER BY REVIEW_REGDATE DESC
